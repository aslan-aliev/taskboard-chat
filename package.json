{
  "name": "taskboard",
  "version": "1.0.0",
  "license": "ISC",
  "scripts": {
    "build": "cd client && npm install && npm run build",
    "start": "node server/index.js"
  },
  "devDependencies": {
    "ngrok": "^5.0.0",
    "wait-on": "^7.2.0"
  }
}
